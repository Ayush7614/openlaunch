# Build context for Dockerfile / `fly deploy`. Dependencies are installed in the
# image (npm ci) and the app is built there; never ship local artifacts or env.
node_modules
.next
.vercel
.env
.env.*
!.env.example
!.env.production.example
*.tsbuildinfo
.DS_Store
.git
.uploads/
